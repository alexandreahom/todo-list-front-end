
// import * as profileService from '../../services/profileService'

const Profile = (props) => {
console.log(props.user.profile)
  return (
    <>
      {/* <h3>{props.user.profiles.todos?.map(todo =>
      <>
        {todo.name}
      </>
        )}</h3> */}
    </>
  )
}

export default Profile