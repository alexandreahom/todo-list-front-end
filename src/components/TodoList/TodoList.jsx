const TodoList = (props) => {
  return ( 
    <>
      {/* <ul>
        {props.todos?.map(todo=>
        <>
            <li>{todo.description}</li>
        </>
            )}
      </ul> */}
    </>
  )
}

export default TodoList;